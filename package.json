{
	"name": "wc-min-max-quantities",
	"private": true,
	"scripts": {
		"css": "sass assets/src:assets/build",
		"start": "npm run css -- --watch",
		"build": "npm run css -- --style=compressed --no-source-map",
		"update": "npx npm-check-updates -u && npm i"
	},
	"devDependencies": {
		"sass": "^1.100.0"
	}
}
